from subprocess import call

KB = 1024
MB = 1024 * 1024
sizes = [ 512, 1 * KB, 4 * KB, 8 * KB, 16 * KB, 32 * KB, 1 * MB, 2 * MB, 4 * MB ]

# 2.2 write_blocks_seq and write_lines
#call(["./exec, "args", "to", "exec"])
